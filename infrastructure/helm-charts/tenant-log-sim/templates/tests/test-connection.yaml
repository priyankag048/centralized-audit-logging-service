apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tenant-log-sim.fullname" . }}-test-connection"
  labels:
    {{- include "tenant-log-sim.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tenant-log-sim.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
