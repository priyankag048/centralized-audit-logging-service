apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenant-python-processor.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  selector:
    app: {{ include "tenant-python-processor.name" . }}
