apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-migrate.fullname" . }}-test-connection"
  labels:
    {{- include "db-migrate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-migrate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
